/**
 * @fileoverview Entry point for ./styles subpath.
 * StyleBuilder + themes + presets + StyleManager.
 */
export { StyleBuilder, StylePresets, THEME_PRESETS } from './styling/index.js';
export { createStyleManager, type StyleManager, type DisplaySettings, type PresetState } from './styles/index.js';
